spring:
  domain-datasource:
    pool-name: domain-db
    jdbc-url: jdbc:h2:mem:testdb
    username: sa
    password: ""
    driver-class-name: org.h2.Driver

  batch-datasource:
    pool-name: batch-db
    jdbc-url: jdbc:h2:mem:testdb
    username: sa
    password: ""
    driver-class-name: org.h2.Driver

  data:
    redis:
      host: localhost
      port: 6379
      connect-timeout: 5s
      timeout: 5s